<div class="airline-preview" data-project-preview aria-hidden="true">
  <div class="inbox-line line-one"></div>
  <div class="inbox-line line-two"></div>

  <div class="message incoming">
    <span class="avatar">@</span>
    <span class="message-copy"><i></i><i></i><i></i></span>
    <span class="sentiment-scan"></span>
  </div>

  <div class="message response">
    <span class="bot-reply">
      <i class="speech-line speech-one"></i>
      <i class="speech-line speech-two"></i>
      <i class="speech-line speech-three"></i>
      <span class="bot-head">
        <span class="bot-antenna"></span>
        <i></i><i></i>
      </span>
    </span>
    <span class="message-copy"><i></i><i></i></span>
  </div>
</div>

<style>
  .airline-preview {
    position: relative;
    container-type: inline-size;
    width: 100%;
    aspect-ratio: 1.75;
    overflow: hidden;
    border: 1px solid var(--border);
    border-radius: var(--radius);
    background:
      radial-gradient(circle at 18% 18%, color-mix(in srgb, #778da0 14%, transparent), transparent 38%),
      color-mix(in srgb, var(--surface) 82%, var(--background));
    box-shadow: var(--shadow);
  }

  .inbox-line {
    position: absolute;
    width: 66cqw;
    height: 25cqw;
    border: 1px solid color-mix(in srgb, #71879a 24%, transparent);
    border-top: 0;
    border-left: 0;
    border-radius: 0 0 40cqw;
    opacity: 0.45;
  }

  .line-one { top: -6.5cqw; right: 12cqw; transform: rotate(-8deg); }
  .line-two { bottom: -14.5cqw; left: 5.5cqw; transform: rotate(172deg); }

  .message {
    position: absolute;
    display: grid;
    grid-template-columns: 11.5cqw minmax(0, 1fr);
    gap: 4.5cqw;
    align-items: center;
    border: 1px solid color-mix(in srgb, #667d90 42%, var(--border));
    border-radius: 7.2cqw;
    background: color-mix(in srgb, var(--background) 74%, var(--surface));
    box-shadow: 0 3.5cqw 10cqw rgb(20 31 38 / 8%);
  }

  .incoming {
    top: 50%;
    left: 9cqw;
    width: 61cqw;
    padding: 6.2cqw 7cqw;
    transform: translateY(-50%);
    transition: transform 520ms cubic-bezier(0.2, 0.8, 0.2, 1);
  }

  .avatar {
    display: grid;
    width: 11.5cqw;
    aspect-ratio: 1;
    place-items: center;
    border-radius: 50%;
    background: color-mix(in srgb, #71879a 15%, var(--surface));
    color: #5e7487;
    font-family: "Inter Variable", sans-serif;
    font-size: 5.2cqw;
    font-weight: 600;
    line-height: 1;
  }

  .message-copy { display: grid; gap: 2.2cqw; }
  .message-copy i { display: block; height: 1.8cqw; border-radius: 999px; background: color-mix(in srgb, var(--muted) 38%, transparent); }
  .message-copy i:nth-child(2) { width: 82%; }
  .message-copy i:nth-child(3) { width: 54%; }

  .sentiment-scan {
    position: absolute;
    top: 4.2cqw;
    bottom: 4.2cqw;
    left: 4.5cqw;
    width: 1.3cqw;
    border-radius: 999px;
    background: color-mix(in srgb, #b97970 72%, var(--surface));
    opacity: 0;
    transform: scaleY(0.2);
  }

  .response {
    right: 7.2cqw;
    bottom: 7cqw;
    width: 53.5cqw;
    padding: 5.5cqw 6.2cqw;
    opacity: 0;
    transform: translateX(11.5cqw) translateY(2.5cqw);
  }

  .bot-reply {
    position: relative;
    display: block;
    width: 11.5cqw;
    height: 10cqw;
  }

  .bot-head {
    position: absolute;
    top: 2.2cqw;
    right: 0;
    display: block;
    box-sizing: border-box;
    width: 7cqw;
    height: 5.6cqw;
    border: 1px solid #667d90;
    border-radius: 1.7cqw;
    background: color-mix(in srgb, #71879a 9%, var(--background));
  }

  .bot-antenna {
    position: absolute;
    bottom: 100%;
    right: 0;
    left: 0;
    z-index: -1;
    display: block;
    width: 1.05cqw;
    height: 2.2cqw;
    margin-inline: auto;
  }

  .bot-antenna::before {
    position: absolute;
    top: 0;
    right: 0;
    left: 0;
    width: 1.05cqw;
    aspect-ratio: 1;
    margin-inline: auto;
    border-radius: 50%;
    background: #667d90;
    content: "";
  }

  .bot-antenna::after {
    position: absolute;
    top: 0.92cqw;
    bottom: 0;
    right: 0;
    left: 0;
    width: 1px;
    margin-inline: auto;
    border-radius: 999px;
    background: #667d90;
    content: "";
  }

  .bot-head { z-index: 1; }

  .bot-head i {
    position: absolute;
    top: 2cqw;
    display: block;
    width: 1.05cqw;
    aspect-ratio: 1;
    border-radius: 50%;
    background: #667d90;
  }

  .bot-head i:first-child { left: 1.55cqw; }
  .bot-head i:last-child { right: 1.55cqw; }

  .speech-line {
    position: absolute;
    left: 1.05cqw;
    display: block;
    width: 2.7cqw;
    height: 1px;
    border-radius: 999px;
    background: #667d90;
    transform-origin: right;
  }

  .speech-one { top: 2.85cqw; transform: rotate(24deg); }
  .speech-two { top: 4.9cqw; }
  .speech-three { top: 6.95cqw; transform: rotate(-24deg); }

  :global(.project-card:hover) .incoming,
  :global(.project-card.is-preview-active) .incoming,
  :global(.project-card:focus-visible) .incoming { transform: translate(-1.8cqw, -68%); }

  :global(.project-card:hover) .sentiment-scan,
  :global(.project-card.is-preview-active) .sentiment-scan,
  :global(.project-card:focus-visible) .sentiment-scan { animation: scan-message 480ms 120ms ease both; }

  :global(.project-card:hover) .response,
  :global(.project-card.is-preview-active) .response,
  :global(.project-card:focus-visible) .response { animation: reveal-response 520ms 440ms cubic-bezier(0.16, 0.82, 0.28, 1) both; }

  @keyframes scan-message {
    0% { left: 4.5cqw; opacity: 0; transform: scaleY(0.2); }
    24% { opacity: 0.82; transform: scaleY(1); }
    78% { opacity: 0.82; }
    100% { left: calc(100% - 5.5cqw); opacity: 0; transform: scaleY(0.35); }
  }

  @keyframes reveal-response {
    from { opacity: 0; transform: translateX(11.5cqw) translateY(2.5cqw); }
    to { opacity: 1; transform: translateX(0) translateY(0); }
  }

  @media (prefers-reduced-motion: reduce) {
    .incoming { transform: translate(-1.8cqw, -68%); }
    .response { opacity: 1; transform: none; }
    .sentiment-scan { display: none; }
  }
</style>
